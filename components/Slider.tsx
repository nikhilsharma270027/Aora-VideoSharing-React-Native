import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Slider = () => {
    
  return (
    <View>
        <Text style={styles.text}>Hey sharma</Text>
    </View>
  )
}

export default Slider

// @ts-ignore
const styles = StyleSheet.create({
    text : {
        color: 'white',
    },
});
